extends layout

block content
    div(class="chat-page")
        div(class="bar")
            h3 FSE Chat Room
            form(action="/logout", method="post", style="margin-left:auto;") 
                button Logout
        div(id="messages")
        form(id="chat-form", action="")
            input(id="input")
            div(class="bar")
                button(style="margin: auto") Post

    script(src="/socket.io/socket.io.js")
    script.
        var user = !{JSON.stringify(user)};
    script(src="javascripts/chat.js")
